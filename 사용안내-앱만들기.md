# 우리 모임 — 네이티브 앱 (안드로이드 + 아이폰)

같은 코드 하나로 **안드로이드 앱과 아이폰 앱**이 나옵니다.
데이터는 **기존 웹앱(apsan-badminton.netlify.app)과 같은 Firebase 방**을 쓰기 때문에,
지금 쓰는 모임·회원·대화·회비가 앱에서 그대로 보입니다.

| 회원이 쓰는 것 | 되는 것 |
|---|---|
| 안드로이드 앱 | 전부 (푸시 알림 포함) |
| 아이폰 앱 | 전부 (푸시 알림 포함) — 애플 개발자 가입 후 |
| 웹 (지금 쓰는 주소) | 전부 — 앱을 안 깔아도 그대로 |

셋이 **같은 방에 섞여** 있어도 됩니다.

---

## 안드로이드 — 지금 바로 됩니다

⚠️ 이 앱은 하나의 코드로 **두 가지 앱**을 만듭니다(`woori` = 판매용 「우리 모임」,
`apsan` = 앞산 배드민턴 전용). 그래서 **어느 갈래인지 반드시 적어야** 합니다.
안 적으면 「.apk 파일을 못 찾겠다」며 그 자리에서 멈춥니다.

```
cd C:\Users\asas3\Desktop\woorimoim
flutter build apk --release --flavor woori
```
나오는 곳: `build\app\outputs\flutter-apk\app-woori-release.apk`

앞산 회원들에게 보낼 것:
```
flutter build apk --release --flavor apsan
```
나오는 곳: `build\app\outputs\flutter-apk\app-apsan-release.apk`

플레이스토어에 올릴 때는 APK 대신 AAB:
```
flutter build appbundle --release --flavor woori
```
나오는 곳: `build\app\outputs\bundle\wooriRelease\app-woori-release.aab`

**서명 열쇠**는 `android\woorimoim.keystore` (비번 woorimoim2026, alias woorimoim).
어제 만든 TWA APK와 **같은 열쇠**라서, 그걸 이미 깐 분에게는 업데이트로 설치됩니다.
⚠️ 이 파일을 잃어버리면 앱 업데이트를 영영 못 만듭니다.

⚠️ **이 열쇠는 GitHub에 안 올라갑니다.** `android\.gitignore` 가 일부러 막아 두었습니다
(열쇠가 새면 남이 이 앱의 「업데이트」를 만들 수 있습니다).
그래서 클라우드 빌드(Codemagic)를 쓸 때는 **Codemagic 화면에 열쇠를 따로 올려야** 합니다:
[Teams > Code signing identities] 에 `woorimoim.keystore` 를 올리고,
`codemagic.yaml` 의 `android_signing` 참조 이름을 그것과 맞춥니다.

열쇠 없이 빌드하면 **오류가 안 나고** 「서명 안 된」 앱이 나옵니다 —
폰에 안 깔리고 스토어도 안 받는데 그때서야 압니다.
그래서 CI 에 «열쇠가 없으면 그 자리에서 멈추는» 단계를 넣어 두었습니다.

---

## 아이폰 — 맥이 필요합니다

아이폰 앱은 애플 규정상 **맥(macOS)에서만 최종 빌드**가 됩니다. 윈도우에서는 안 됩니다.
코드·설정은 전부 준비해 두었으니, 아래 셋 중 하나만 고르면 됩니다.

### 방법 1. 클라우드 맥 (추천 — 맥을 안 사도 됨)
`codemagic.yaml` 파일을 이미 만들어 두었습니다.
1. 이 폴더를 GitHub **비공개** 저장소에 올린다
2. codemagic.io 에 GitHub 계정으로 로그인 → 이 저장소 선택
3. 애플 개발자 계정을 연결 (App Store Connect API 키)
4. [Start new build] → 클라우드 맥이 아이폰 앱(.ipa)을 만들어 테스트플라이트까지 올려줍니다

무료 사용량이 매달 있고, 넘으면 분당 과금입니다.

### 방법 2. 맥 빌리기
MacinCloud 같은 곳에서 시간당 빌려 Xcode로 빌드.

### 방법 3. 맥 사기
Mac mini 정도면 충분합니다. 자주 업데이트할 계획이면 이게 편합니다.

### 어느 방법이든 반드시 필요한 것
- **애플 개발자 프로그램** 연 $99 (약 13만원) — 없으면 앱스토어 배포 불가
- **APNs 키** (푸시 알림용): 애플 개발자 사이트 → Keys → [+] → Apple Push Notifications service 체크 →
  받은 `.p8` 파일을 Firebase 콘솔 → 프로젝트 설정 → 클라우드 메시징 → Apple 앱 구성에 올리기
  (이걸 안 하면 아이폰에서 알림만 안 옵니다. 나머지 기능은 다 됩니다)

---

## 이미 해둔 것 (다시 안 해도 됨)

- ✅ Firebase에 안드로이드 앱 등록 + `google-services.json`
- ✅ Firebase에 아이폰 앱 등록 + `GoogleService-Info.plist`
- ✅ 번들 ID: 안드로이드는 갈래별로 `com.taejinsoft.woorimoim`(판매용) / `com.taejinsoft.apsanclub`(앞산),
  아이폰은 아직 `com.taejinsoft.woorimoim` 하나 (아래 「아이폰은 아직 하나뿐」 참고)
- ✅ 아이폰 권한 안내문 (사진첩·카메라) — 없으면 앱이 튕깁니다
- ✅ 아이폰 백그라운드 알림 설정
- ✅ 안드로이드 알림 권한 + 알림 채널
- ✅ 서명 설정 (안드로이드)
- ✅ 앱 이름 한글: 안드로이드는 갈래별로 「우리 모임」/「앞산 배드민턴」, 아이폰은 「우리 모임」
- ✅ 세로 화면 고정

---

## 서버는 손댈 것이 없습니다

푸시를 보내는 함수(`pushOnMsgApsan`)는 이미 돌고 있고,
앱은 웹앱과 **똑같은 자리**(`push.{uid}.token`)에 토큰을 적기 때문에 그대로 동작합니다.
안드로이드·아이폰·웹 어디서 보내도 나머지 전원에게 알림이 갑니다.

---

## 요금

Firestore 요금은 **읽고 쓴 횟수**로 매겨지므로 앱이든 웹이든 같습니다.
오히려 앱은 로컬 캐시가 기본으로 켜져 있어 다시 열 때 서버를 덜 부릅니다.

---

## ⚠️ 아이폰 알림에는 서버 한 줄이 더 필요합니다

지금 푸시 서버(`pushOnMsgApsan`)는 웹 브라우저에 맞춰 **데이터만** 보냅니다.

| | 지금 상태 |
|---|---|
| 웹 | ✅ 잘 됩니다 (서비스워커가 직접 띄움) |
| 안드로이드 앱 | ✅ 잘 됩니다 (앱 안에 받는 자리를 만들어 뒀습니다) |
| 아이폰 앱 | ❌ **알림만** 안 옵니다 (앱 기능은 전부 정상) |

아이폰은 애플 규정상 서버가 「보여줄 문구」를 함께 보내야만 알림을 띄웁니다.
아이폰 앱을 낼 때 `functions/index.js` 의 보내는 부분에 아래처럼 **apns 칸만 추가**하면 됩니다.
(apns 칸은 아이폰에만 적용돼서 웹·안드로이드는 지금 그대로 동작합니다)

```js
const payload = {
  data: { title, body, tag: 'club-msg', url: './index.html?go=chat' },
  apns: {                                   // ← 이 칸만 추가
    payload: { aps: { alert: { title, body }, sound: 'default' } },
  },
};
```

고친 뒤 배포는 반드시 함수 이름까지 붙여서:
```
firebase deploy --only functions:pushOnMsgApsan
```
(이름을 안 붙이면 커플 앱의 다른 함수를 지우겠냐고 묻습니다)


---

## ⚠️ 아이폰은 아직 「우리 모임」 하나뿐입니다

안드로이드는 앱 두 가지(우리 모임 / 앞산 배드민턴)로 나눠 뒀지만,
**아이폰은 아직 나누지 않았습니다.** Xcode에서 구성(scheme)을 만들어야 해서 맥이 필요하기 때문입니다.

맥에서 아이폰 앱을 만들 때 함께 해야 할 일:
1. Xcode에서 scheme 두 개(Runner-woori / Runner-apsan) 만들기
2. 각각 번들 ID를 `com.taejinsoft.woorimoim` / `com.taejinsoft.apsanclub` 로
3. Firebase에 **앞산용 아이폰 앱을 추가 등록**하고 `GoogleService-Info.plist` 를 갈래별로 넣기
4. `lib/config.dart` 의 `_ios` 도 갈래에 따라 고르도록 (안드로이드 `_android` 와 같은 방식)

⚠️ 4번을 빠뜨리면 **앞산 아이폰 앱이 「우리 모임」의 열쇠로 등록되어 알림이 안 옵니다.**
(안드로이드에서 실제로 그랬던 것을 27회차 점검에서 잡았습니다)

---

## ⚠️ 알림(푸시) 서버 — 배포할 때 이름을 콕 집으세요

앱의 알림은 서버에 올려둔 작은 프로그램 `pushOnMsgApsan` 이 보냅니다.
그 원본은 이 앱 폴더가 아니라 아래에 있습니다:

```
Desktop/앞산배드민턴/functions/index.js
```

커플앱(데이트장부)도 **같은 Firebase 계정**을 쓰는데, 함수 원본은 폴더가 갈라져 있습니다.
그래서 이름 없이 통째로 올리면 **다른 쪽 함수가 지워집니다.**

**규칙 (커플앱 쪽 `규칙점검.md`에 이미 정해져 있는 것과 같습니다):**

```bash
firebase deploy --only functions:pushOnMsgApsan
```

이렇게 **이름을 지정해서** 올리면 안전합니다.
알림이 죽어도 앱에는 「알림을 켰어요 🔔」 라고 그대로 나오므로 눈치채기 어렵습니다.
살아 있는지 확인은 `Desktop/데이트장부`에서 `node 서버상태.mjs` 로 하면 함수 3개를 한 번에 점검해줍니다.

(규칙 파일 `firestore.rules` 는 세 앱 것이 한 파일에 다 들어 있어서 이 문제가 없습니다.
 함수만 폴더가 갈라져 있습니다.)

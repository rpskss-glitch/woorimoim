# 앱 두 가지 만들기 (하나의 소스)

| | 판매용 | 앞산 배드민턴 동호회용 |
|---|---|---|
| 앱 이름 | 우리 모임 | 앞산 배드민턴 |
| 꾸러미 이름 | com.taejinsoft.woorimoim | com.taejinsoft.apsanclub |
| 만들 때 | `--flavor woori` | `--flavor apsan` |

**기능은 완전히 같습니다.** 이름·아이콘·꾸러미만 다르고, 데이터는 같은 곳을 봅니다.
그래서 두 앱을 쓰는 사람이 **같은 모임에 섞여 있어도** 됩니다.

> 💡 소스가 하나라서 **버그를 한 번 고치면 두 앱에 모두 적용**됩니다.
> (폴더를 복사했다면 매번 두 번 고쳐야 하고, 시간이 지나면 두 앱이 서로 달라집니다)

## 만드는 명령

앞산 배드민턴 (회원들에게 보낼 것):
```
flutter build apk --release --flavor apsan
```
→ `build\app\outputs\flutter-apk\app-apsan-release.apk`

판매용 우리 모임:
```
flutter build apk --release --flavor woori
```
→ `build\app\outputs\flutter-apk\app-woori-release.apk`

플레이스토어용(AAB)은 `apk`를 `appbundle`로 바꾸면 됩니다.

⚠️ **갈래를 안 적으면 빌드가 안 됩니다** — 어느 앱을 만들지 정해야 하기 때문입니다.

> 💡 예전에는 `--dart-define=BRAND=…` 도 함께 적어야 했습니다. 그걸 빠뜨리면
> **겉은 앞산인데 속은 우리 모임인 앱**(이름·Firebase 열쇠가 어긋난 앱)이 조용히 나왔습니다.
> 이제 앱이 **꾸러미 이름을 보고 스스로 알아내서** 어긋날 수가 없습니다.

## 아이콘
지금은 두 앱이 같은 아이콘을 씁니다.
앞산 배드민턴용 아이콘을 따로 주시면 `android/app/src/apsan/res/` 에 넣어 구분되게 하겠습니다.

## 아이폰
아이폰도 같은 방식으로 나눌 수 있지만, **Xcode에서 구성(scheme)을 만들어야 해서 맥이 필요**합니다.
맥에서 빌드할 때 함께 처리하면 됩니다.

## Firebase
두 꾸러미 이름을 각각 등록해 두었습니다 (알림이 양쪽 다 오게 하려면 필요).
- 우리 모임: `android/app/src/woori/google-services.json`
- 앞산 배드민턴: `android/app/src/apsan/google-services.json`
